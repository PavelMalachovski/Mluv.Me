# Быстрый тест: Настройки языка и темы

## ✅ Что добавлено

1. **Переключатель языка** (Русский ⇄ Українська)
2. **Исправленное переключение темы** (Светлая ⇄ Тёмная)

## 🧪 Как протестировать после деплоя

### Тест 1: Переключение темы

1. Откройте **Settings → Appearance** (Настройки → Внешний вид)
2. В самом верху видите секцию "Тема / Theme"
3. Кликните на **"Светлая"**
   - ✅ Должна активироваться светлая тема (белый фон)
   - ✅ Галочка должна появиться на кнопке "Светлая"
4. Кликните на **"Тёмная"**
   - ✅ Должна активироваться тёмная тема (темный фон)
   - ✅ Галочка должна переместиться на кнопку "Тёмная"
5. Обновите страницу (F5)
   - ✅ Тема должна сохраниться

### Тест 2: Переключение языка

1. В той же вкладке **Settings → Appearance**
2. Прокрутите вниз до секции "Язык интерфейса / Interface Language"
3. Кликните на **🇷🇺 Русский**
   - ✅ Должен появиться спиннер загрузки
   - ✅ Должно появиться уведомление "Profile updated"
   - ✅ Галочка должна появиться на кнопке "Русский"
4. Кликните на **🇺🇦 Українська**
   - ✅ Должен появиться спиннер загрузки
   - ✅ Должно появиться уведомление "Profile updated"
   - ✅ Галочка должна переместиться на кнопку "Українська"
5. Обновите страницу (F5)
   - ✅ Язык должен сохраниться

### Тест 3: Проверка сохранения в базе

1. Откройте **DevTools (F12) → Network**
2. Переключите язык или тему
3. Найдите запрос `PATCH /api/v1/users/{id}`
4. ✅ Должен вернуть статус 200
5. ✅ В ответе должно быть поле `ui_language: "ru"` или `"uk"`

### Тест 4: Кросс-страничный тест

1. Переключите тему на **тёмную**
2. Перейдите на **Profile**
   - ✅ Тема должна остаться тёмной
3. Перейдите на **Saved**
   - ✅ Тема должна остаться тёмной
4. Вернитесь в **Settings**
   - ✅ Галочка должна быть на "Тёмная"

## 🐛 Что проверить если не работает

### Если тема не переключается:

1. **Откройте Console (F12)**
   - Есть ли ошибки JavaScript?

2. **Проверьте HTML элемент**
   ```javascript
   console.log(document.documentElement.classList)
   // Должно содержать 'light' или 'dark'
   ```

3. **Проверьте localStorage**
   ```javascript
   console.log(localStorage.getItem('theme-storage'))
   // Должно быть: {"state":{"theme":"dark"},...}
   ```

### Если язык не меняется:

1. **Откройте Network tab (F12)**
   - Запрос уходит?
   - Какой статус ответа? (должен быть 200)

2. **Проверьте ответ API**
   - В ответе есть `ui_language`?
   - Правильное ли значение (`"ru"` или `"uk"`)?

3. **Проверьте auth store**
   ```javascript
   console.log(localStorage.getItem('auth-storage'))
   // Должно содержать ui_language: "ru" или "uk"
   ```

## 📍 Где находятся настройки

**Desktop:**
- Sidebar слева → Settings (⚙️)
- Вкладка "Appearance"

**Mobile:**
- Навигация снизу → Settings (⚙️)
- Вкладка "Appearance"

## 🎨 Как выглядит UI

### Секция темы:
```
┌─────────────────────────────────┐
│ 🌞 Тема / Theme                 │
│                                  │
│ ┌─────────┐    ┌─────────┐     │
│ │ ☀️       │    │ 🌙       │     │
│ │ Светлая │    │ Тёмная  │     │
│ │ Bright  │    │ Dark     │     │
│ │      ✓  │    │          │     │
│ └─────────┘    └─────────┘     │
└─────────────────────────────────┘
```

### Секция языка:
```
┌─────────────────────────────────┐
│ 🌍 Язык интерфейса              │
│                                  │
│ ┌─────────┐    ┌─────────┐     │
│ │ 🇷🇺      │    │ 🇺🇦      │     │
│ │ Русский │    │Українська│     │
│ │ Russian │    │Ukrainian │     │
│ │      ✓  │    │          │     │
│ └─────────┘    └─────────┘     │
└─────────────────────────────────┘
```

## ✅ Ожидаемый результат

После теста:
- ✅ Тема переключается мгновенно
- ✅ Язык сохраняется в базе данных
- ✅ Настройки сохраняются после перезагрузки
- ✅ Все работает в светлой и темной теме
- ✅ Нет ошибок в консоли
- ✅ API запросы успешны (200 OK)

## 📝 Примечания

- **Тема** сохраняется локально (localStorage)
- **Язык** сохраняется на сервере (база данных)
- Оба параметра переживают logout/login
- Изменения применяются мгновенно
